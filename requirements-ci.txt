# Minimal dependencies for the CI test suite.
# Does NOT include ML, dashboard, or broker packages — none are needed for tests.
# Full stack: requirements.txt

pytest>=8.2.0
pytest-asyncio>=0.23.0
requests>=2.31.0
numpy>=1.26.0
pandas>=2.2.0
