# ── Test framework ────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
requests-mock>=1.12.0

# ── Core data / math ──────────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.12.0
pandas>=2.2.0
scikit-learn>=1.4.0
hmmlearn>=0.3.0
statsmodels>=0.14.0
ta>=0.11.0
arch>=6.3.0

# ── Market data ───────────────────────────────────────────────────────────────
yfinance>=0.2.38
pandas-datareader>=0.10.0

# ── Data validation / IO ──────────────────────────────────────────────────────
pydantic>=2.5.0
openpyxl>=3.1.0
requests>=2.31.0
urllib3>=2.0.0
beautifulsoup4>=4.12.0
lxml>=5.1.0
nltk>=3.8.0
pyarrow>=14.0.0

# ── Logging / config ──────────────────────────────────────────────────────────
loguru>=0.7.2
python-dotenv>=1.0.0

# ── Intelligence cache ────────────────────────────────────────────────────────
diskcache>=5.6.0

# ── Claude API ────────────────────────────────────────────────────────────────
anthropic>=0.28.0

# Excluded from CI (stubbed in tests or pipeline-only):
#   alpaca-py       — broker SDK, faked with SimpleNamespace/MagicMock
#   streamlit       — dashboard, stubbed via sys.modules
#   plotly          — dashboard charts, not exercised in unit tests
#   sentence-transformers — heavy embeddings model, no CI test coverage
#   redis           — hot-tier cache, mocked at call sites
#   fastapi / uvicorn / httpx — backend REST server, not in tests/
